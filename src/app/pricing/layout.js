export const metadata = {
  title: "Pricing Packages",
  description: "Flexible, transparent monthly plans tailored for freelancers, teams, and enterprises. Calculate billing savings with BillNode.",
};

export default function PricingLayout({ children }) {
  return children;
}
