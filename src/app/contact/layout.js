export const metadata = {
  title: "Contact Operations",
  description: "Connect with the BillNode team for priority support, API integrations, and customized enterprise billing setups.",
};

export default function ContactLayout({ children }) {
  return children;
}
